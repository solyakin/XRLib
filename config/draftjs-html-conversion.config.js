export const DEFAULT_HTML_CONVERSION_OPTIONS = {
    inlineStyles: {
        LIST: {
            attributes: { fontFamily: "Firs" }
        },
        // Override default element (`strong`).
        /*BOLD: { //element: "b",
                attributes: {class:"suz"}},*/
        /*  ITALIC: {
           // Add custom attributes. You can also use React-style `className`.
           attributes: { class: 'is-italic' }, // just a test
           // Use camel-case. Units (`px`) will be added where necessary.
         },
         UNDERLINE: {
           attributes: { class: 'is-underline' },
         },
       //TODO: add more inline styles
     
         LIST:{},
         // Use a custom inline style. Default element is `span`.
         RED: { style: { color: "#900" } }, */
    },
};